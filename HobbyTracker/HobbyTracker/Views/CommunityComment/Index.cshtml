@model IEnumerable<HobbyTracker.Models.CommunityComment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Comment.TextInput)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Community.CommunityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comment.TextInput)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Community.CommunityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommunityCommentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommunityCommentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommunityCommentID })
        </td>
    </tr>
}

</table>
