@model HobbyTracker.ViewModels.CollectionIndexData

@{
    ViewBag.Title = "Collections";
}
<head>
    <link href="~/Content/bootstrap.css" rel="stylesheet">
    <link href="~/Content/bootstrap-theme.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="~/Content/styles.css" />
    <title>Collections</title>
</head>

<body>
    <div class=" panel panel-warning">
        <div class="panel-heading text-center"><h2>My Collection Page</h2></div>
        <div class="panel-bady">

            <p class="text-danger">
                @Html.ActionLink("Create A New Collection", "Create") <br />
                @Html.ActionLink("Browse Items", "Index", "Item") <br />
                @Html.ActionLink("Create A New Item", "Create", "Item") <br />
            </p>

            <div class="panel-heading text-center"><h2>My Collections</h2></div>

            <table class="table table-hover">
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Genre
                    </th>
                    <th>
                        Private?
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model.Collections)
                {
                    string selectedRow = "";
                    if (item.CollectionID == ViewBag.CollectionID)
                    {
                        selectedRow = "success";
                    }
                    <tr class="@selectedRow">
                        <td>
                            @Html.ActionLink(item.CollectionName, "Index", new { collectionID = item.CollectionID })
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Genre.GenreName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Private)
                        </td>
                        <td>
                            @Html.ActionLink("Add Item", "Create", "CollectionItem", new { id = item.CollectionID }, new { }) |
                            @Html.ActionLink("Edit", "Edit", new { id = item.CollectionID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.CollectionID })
                        </td>
                    </tr>
                }
            </table>

            @if (Model.CollectionItems != null)
            {

                <div class="panel-heading text-center"><h2>Items in the Selected Collection</h2></div>
                <table class="table table-bordered table table-striped table-hover">
                    <tr>
                        <th>Item Name</th>
                        <th>Description</th>
                        <th>Notes</th>
                        <th>I own?</th>
                        <th></th>
                    </tr>
                    @foreach (var item in Model.CollectionItems)
                    {
                        <tr align="center">
                            <td>
                                @item.Item.ItemName
                            </td>
                            <td>
                                @item.Item.ItemDesc
                            </td>
                            <td>
                                @item.Note
                            </td>
                           
                            <td>
                                @item.IOwn
                            </td>
                            <td>
                                @Html.ActionLink("Delete this Item", "Delete", "CollectionItem", new { id = item.CollectionItemID }, null)
                            </td>
                            <td>
                                @Html.ActionLink("Edit Notes", "Edit", "CollectionItem", new { id = item.CollectionItemID }, null)
                            </td>
                        </tr>
                    }
                </table>
            }
        </div>
    </div>
</body>
