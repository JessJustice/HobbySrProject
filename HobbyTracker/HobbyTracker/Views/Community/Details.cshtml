@model HobbyTracker.Models.Community

@{
    ViewBag.Title = "Details";
}

<h2>@Html.DisplayFor(model => model.CommunityName) Community</h2>

<p>
    Description <br />
    @Html.DisplayFor(model => model.DescriptionField)

</p>


<div>
    <h4>Details:</h4>
    <p>This section is under construction. Please check back later for additional features!</p>
    <hr />
    <h4>Comments:</h4>
    <dl class="dl-horizontal">
        <dd>
            <table class="table">
                <tr>
                    <th></th>
                </tr>
                @foreach (var item in Model.Comments)
                {
                    <tr>
                        <td>
                            @item.TextInput
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>
